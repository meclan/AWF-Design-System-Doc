import React from 'react'
import StubPage from '../components/StubPage.jsx'

export default function ComponentOverlay() {
  return (
    <StubPage
      name="Overlay"
      category="Overlay"
      tokenPrefix="modal"
      description="Modal dialogs, popovers, and tooltips that appear above the page content. Modals interrupt the user flow for critical actions or confirmations. Popovers and tooltips provide contextual supplementary information."
    />
  )
}
