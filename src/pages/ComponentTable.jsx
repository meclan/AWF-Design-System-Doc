import React from 'react'
import StubPage from '../components/StubPage.jsx'

export default function ComponentTable() {
  return (
    <StubPage
      name="Table"
      category="Display"
      tokenPrefix="table"
      description="Tabular data component with row selection, multi-column sorting, bulk action bar, contextual row menus, and detachable pagination. Available in Default and High Contrast variants."
    />
  )
}
