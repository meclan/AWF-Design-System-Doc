import React from 'react'
import StubPage from '../components/StubPage.jsx'

export default function ComponentNavbar() {
  return (
    <StubPage
      name="Navbar"
      category="Navigation"
      tokenPrefix="navbar"
      description="Application-level navigation bar anchored at the top of the viewport. Contains product branding, primary navigation items, and user account actions. Adapts colors per product theme."
    />
  )
}
