import React from 'react'
import StubPage from '../components/StubPage.jsx'

export default function ComponentNav() {
  return (
    <StubPage
      name="Nav Controls"
      category="Navigation"
      tokenPrefix="pagination"
      description="Pagination controls and navigation utilities for moving through paginated datasets. Includes previous/next buttons, page number indicators, and items-per-page selectors."
    />
  )
}
