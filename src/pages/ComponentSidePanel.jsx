import React from 'react'
import StubPage from '../components/StubPage.jsx'

export default function ComponentSidePanel() {
  return (
    <StubPage
      name="Side Panel"
      category="Navigation"
      tokenPrefix="sidepanel"
      description="Slide-in drawer panel anchored to the right edge of the viewport. Used for contextual details, inline editing, filters, and settings without leaving the current page."
    />
  )
}
